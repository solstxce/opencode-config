---
description: Hand implementation work to the build agent.
---

Ask the `@build` subagent to implement: $ARGUMENTS

It should delegate execution to `@executor` and review to `@reviewer`/`@expert-reviewer`. Return what changed, files touched, and validation results.
