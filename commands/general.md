---
description: Hand a task to the general orchestrator.
---

Act as the `@general` primary orchestrator for: $ARGUMENTS

Break the request down, delegate implementation to `@build`, planning to `@plan`, and execution/validation to `@executor`. Return a compact summary of what was done and any follow-ups.
