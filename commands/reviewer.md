---
description: Review the changed scope with the reviewer agent.
---

Ask the `@reviewer` subagent to review only the changed scope using the `code-review` skill and write findings to `.agents/tasks/review.md`.

Scope: $ARGUMENTS

Return a compact verdict with blocking vs non-blocking findings.
