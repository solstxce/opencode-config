---
description: Run bash commands, tests, builds, or validation via the executor.
---

Ask the `@executor` subagent to execute: $ARGUMENTS

Run the smallest sufficient set of commands to complete or verify it. Report exact results, failures, and exit statuses.
